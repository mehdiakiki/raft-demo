# syntax=docker/dockerfile:1

# Development Dockerfile - expects binary to be built externally
# This is faster for development iteration

FROM alpine:3.21
RUN apk add --no-cache ca-certificates
COPY gateway /bin/gateway
ENTRYPOINT ["/bin/gateway"]
